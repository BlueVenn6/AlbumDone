# AlbumDone vX.Y.Z

## English

### Highlights

- Describe only changes verified in this release.

### Download

- Windows installer: `AlbumDone-X.Y.Z-BUILD-x64.exe`
- Download the `.exe` asset below. `Source code (zip)` and `Source code (tar.gz)` are source archives, not Windows installers.

### System Requirements

- Windows 10 or Windows 11 on Intel/AMD x64.
- Windows 11 ARM64 may run this x64 build through Windows x64 emulation; this is not an ARM64-native build.
- No Android or iOS download is included in this desktop release.

### Installation

1. Download the Windows `.exe` asset from this Release.
2. Verify its SHA-256 against `SHA256SUMS.txt`.
3. Run the installer and follow the prompts.

### Code-signing Status

The current Windows installer is not code-signed yet. Windows SmartScreen may display a warning. Only continue after confirming the file came from this repository and its checksum matches.

### Safety Notice

Start with a copied test folder or a fully backed-up photo library. Review every similar-photo group and deletion candidate before confirming file operations. Similarity results and AI output are assistance, not safe-delete decisions.

### Known Issues

- List only known limitations that apply to this exact release.

### Checksums

```text
SHA256  AlbumDone-X.Y.Z-BUILD-x64.exe
```

Replace the placeholder only with a checksum calculated from the exact uploaded installer. If unavailable, write `Pending verification`; never invent a checksum.

### Feedback

Report reproducible problems through [GitHub Issues](https://github.com/BlueVenn6/AlbumDone/issues). Do not include private photos, API keys, credentials, or sensitive local paths.

---

## 简体中文

### 本次更新

- 只描述在本次版本中已经验证的变更。

### 下载

- Windows 安装程序：`AlbumDone-X.Y.Z-BUILD-x64.exe`
- 请下载下方 `.exe` 附件。`Source code (zip)` 和 `Source code (tar.gz)` 是源代码压缩包，不是 Windows 安装程序。

### 系统要求

- Intel/AMD x64 电脑上的 Windows 10 或 Windows 11。
- Windows 11 ARM64 可以通过 Windows x64 模拟运行此 x64 版本；这不是 ARM64 原生构建。
- 本桌面版本不包含 Android 或 iOS 下载。

### 安装方法

1. 从本 Release 下载 Windows `.exe` 附件。
2. 使用 `SHA256SUMS.txt` 校验 SHA-256。
3. 运行安装程序并按提示操作。

### 代码签名状态

当前 Windows 安装程序尚未进行代码签名，因此 Windows SmartScreen 可能会显示安全提示。只有在确认文件来自本仓库且校验值一致后才继续安装。

### 安全提示

首先使用复制出来的测试文件夹，或已经完整备份的照片图库。确认文件操作前，逐组审核相似照片和删除候选。相似度结果和 AI 输出只用于辅助，不是安全删除决定。

### 已知问题

- 只列出适用于这个确切版本的真实已知限制。

### 文件校验值

```text
SHA256  AlbumDone-X.Y.Z-BUILD-x64.exe
```

只有从实际上传的安装程序计算出校验值后才能替换占位内容。如果暂时没有，请写“等待验证”，不要虚构校验值。

### 反馈方式

请通过 [GitHub Issues](https://github.com/BlueVenn6/AlbumDone/issues) 报告可复现问题。不要提交私人照片、API Key、凭据或敏感本地路径。
